import {
    // actions
} from '../actions';

const initialState = {

};

export const gameReducer = {state = initialState, action} => {
    switch(action.type) {
        case : 
            return {

            };
        default:
            return state;
    }
};